module.exports = {
    // `27017` is mongo's default port
    // `trackermateo` is the db's name
    database: "mongodb://localhost:27017/trackermateo",
    // the secret for our token
    secret: "secretirijillo"
};